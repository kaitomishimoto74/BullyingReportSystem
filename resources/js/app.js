// ensure these lines exist so the component is compiled into app.js
import './MainScreen.jsx';
import './bootstrap'; // if present
import './UserDashboard.jsx';
import './AdminDashboard.jsx'; // add this line so admin SPA is bundled